# Portfolio

## Créer une branche pour des petites modifications

Depuis la racine du dépôt, exécute :

```bash
git switch -c ma-branche-modifs
```

Puis vérifie que tu es bien sur la nouvelle branche :

```bash
git branch --show-current
```
